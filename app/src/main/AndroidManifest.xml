<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rezonans">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Devices.Bbox.Bbox00401Activity"></activity>
        <activity android:name=".Devices.Bbox.Bbox01602Activity"></activity>
        <activity android:name=".Sites.SitesActivity" />
        <activity android:name=".Logon.BarCodeReaderActivity" />
        <activity android:name=".Logon.LogonActivity" />
        <activity android:name=".Main.MainActivity" />
        <activity android:name=".HeadpieceActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>